# Docker Environment Variables
# Copy this to .env.local and add your API key

# Optional: Anthropic API Key for LLM orchestration
# Leave empty to run in standalone mode (no LLM features)
ANTHROPIC_API_KEY=

# All other configuration is handled by docker-compose.yml
